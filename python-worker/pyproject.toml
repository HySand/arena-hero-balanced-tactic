[project]
name = "arena-hero-python-strategy-worker"
version = "0.1.0"
description = "Cloudflare Python Durable Object for the shared Arena Hero strategy"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
