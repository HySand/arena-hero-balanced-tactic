{
  "name": "arena-hero-balanced-tactic-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "secrets": "wrangler secret put ARENA_HERO_API_KEY && wrangler secret put ADMIN_CONTROL_SECRET",
    "cf-typegen": "wrangler types",
    "cf-typegen:check": "wrangler types --check",
    "format:check": "prettier --check package.json tsconfig.json eslint.config.js wrangler.jsonc vitest.config.ts README.md src test public",
    "format": "prettier --write package.json tsconfig.json eslint.config.js wrangler.jsonc vitest.config.ts README.md src test public",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --exclude test/simulation.test.ts",
    "test:coverage": "vitest run --coverage --exclude test/simulation.test.ts",
    "simulate:100": "vite-node test/simulation-report.ts",
    "check": "npm run cf-typegen:check && npm run format:check && npm run lint && npm run typecheck && npm test",
    "test:simulation": "vitest run test/simulation.test.ts",
    "check:full": "npm run check && npm run test:simulation",
    "test:coverage:full": "vitest run --coverage"
  },
  "devDependencies": {
    "@eslint/js": "9.32.0",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "3.2.7",
    "eslint": "9.32.0",
    "globals": "16.3.0",
    "prettier": "3.6.2",
    "typescript": "5.8.3",
    "typescript-eslint": "8.38.0",
    "vitest": "3.2.7",
    "wrangler": "4.120.0"
  }
}
