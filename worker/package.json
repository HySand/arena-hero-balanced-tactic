{
  "name": "arena-hero-balanced-tactic-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "deploy:dry-run": "wrangler deploy --dry-run --config wrangler.jsonc",
    "secrets": "wrangler secret put ARENA_HERO_API_KEY --config wrangler.jsonc && wrangler secret put ADMIN_CONTROL_SECRET --config wrangler.jsonc",
    "cf-typegen": "wrangler types --config wrangler.jsonc",
    "cf-typegen:check": "wrangler types --check --config wrangler.jsonc",
    "format:check": "prettier --check package.json tsconfig.json eslint.config.js wrangler.jsonc vitest.config.ts README.md src test",
    "format": "prettier --write package.json tsconfig.json eslint.config.js wrangler.jsonc vitest.config.ts README.md src test",
    "dashboard:check": "node --check ../dashboard/app.js",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "check": "npm run cf-typegen:check && npm run format:check && npm run dashboard:check && npm run lint && npm run typecheck && npm test",
    "check:full": "npm run check",
    "test:coverage:full": "vitest run --coverage"
  },
  "devDependencies": {
    "@eslint/js": "9.32.0",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "3.2.7",
    "eslint": "9.32.0",
    "globals": "16.3.0",
    "prettier": "3.6.2",
    "typescript": "5.8.3",
    "typescript-eslint": "8.38.0",
    "vitest": "3.2.7",
    "wrangler": "4.120.0"
  }
}
